'use client';import {useState} from 'react';import {Shell} from '../../components';import {useStore} from '../../store';export default function StockOut(){const {companies,products,adjustStock}=useStore();const [company,setCompany]=useState(''),[sku,setSku]=useState(''),[qty,setQty]=useState(1),[platform,setPlatform]=useState('Amazon'),[partner,setPartner]=useState('');const p=products.find(x=>x.companyId===company&&x.sku===sku);function save(){if(p&&qty>0&&adjustStock(p.id,qty,'Stock Out',platform,partner,'Manual stock out')){setSku('');setQty(1)}}return <Shell active="Inventory" title="Stock Out"><div className="page-intro"><div><p className="eyebrow">OUTGOING</p><h1>Stock Out</h1><p className="muted">Deduct marketplace or other outgoing units with platform tracking.</p></div></div><section className="card settings-card"><div className="form-grid"><label>Company<select value={company} onChange={e=>setCompany(e.target.value)}><option value="">Select company</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Exact SKU<input value={sku} onChange={e=>setSku(e.target.value)} placeholder="Enter exact Excel SKU"/></label><label>Quantity<input type="number" min="1" value={qty} onChange={e=>setQty(Number(e.target.value))}/></label><label>Platform<select value={platform} onChange={e=>setPlatform(e.target.value)}><option>Amazon</option><option>Flipkart</option><option>Meesho</option><option>Other</option></select></label><label>Shipping Partner<input value={partner} onChange={e=>setPartner(e.target.value)}/></label></div>{p&&<div className="import-note">{p.name||'Unnamed product'} · Current stock {p.stock}</div>}<button className="btn primary" onClick={save}>Save Stock Out</button></section></Shell>}
