'use client';import {useState} from 'react';import {Shell} from '../../components';import {useStore} from '../../store';export default function StockIn(){const {companies,products,adjustStock}=useStore();const [company,setCompany]=useState(''),[sku,setSku]=useState(''),[qty,setQty]=useState(1),[partner,setPartner]=useState('');const p=products.find(x=>x.companyId===company&&x.sku===sku);async function save(){if(p&&qty>0&&await adjustStock(p.id,qty,'Stock In','',partner,'Manual stock in')){setSku('');setQty(1)}}return <Shell active="Inventory" title="Stock In"><div className="page-intro"><div><p className="eyebrow">INCOMING</p><h1>Stock In</h1><p className="muted">Add incoming units to a specific company using the exact Excel SKU.</p></div></div><section className="card settings-card"><div className="form-grid"><label>Company<select value={company} onChange={e=>setCompany(e.target.value)}><option value="">Select company</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Exact SKU<input value={sku} onChange={e=>setSku(e.target.value)} placeholder="e.g. WG-M1-BK"/></label><label>Quantity<input type="number" min="1" value={qty} onChange={e=>setQty(Number(e.target.value))}/></label><label>Supplier / Partner<input value={partner} onChange={e=>setPartner(e.target.value)}/></label></div>{p&&<div className="import-note">{p.name||'Unnamed product'} · Current stock {p.stock}</div>}<button className="btn primary" onClick={save}>Save Stock In</button></section></Shell>}
