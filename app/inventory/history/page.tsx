'use client';
import {useState} from 'react';import {Shell,CompanyNotice} from '../../components';import {useStore} from '../../store';
export default function History(){const {movements,companies,selectedCompanyId}=useStore();const [type,setType]=useState('All');const list=movements.filter(m=>(selectedCompanyId==='all'||m.companyId===selectedCompanyId)&&(type==='All'||m.type===type));return <Shell active="Inventory" title="Stock History"><div className="page-intro"><div><p className="eyebrow">AUDIT TRAIL</p><h1>Stock History</h1><p className="muted">Every manual, scan and imported stock movement is preserved.</p></div></div><CompanyNotice/><div className="toolbar card"><select value={type} onChange={e=>setType(e.target.value)}><option>All</option><option>Stock In</option><option>Stock Out</option><option>Return</option></select><span className="pill">{list.length} movements</span></div><section className="card table-card"><div className="table-wrap"><table><thead><tr><th>Date</th><th>Company</th><th>SKU</th><th>Type</th><th>Qty</th><th>Stock After</th><th>Platform</th><th>Note</th></tr></thead><tbody>{list.map(m=><tr key={m.id}><td>{new Date(m.date).toLocaleString()}</td><td>{companies.find(c=>c.id===m.companyId)?.name}</td><td className="sku-cell"><b>{m.sku}</b></td><td><span className="badge">{m.type}</span></td><td className={m.qty>=0?'success-text':'danger-text'}>{m.qty>=0?'+':''}{m.qty}</td><td>{m.stockAfter}</td><td>{m.platform||'—'}</td><td>{m.note||'—'}</td></tr>)}</tbody></table>{!list.length&&<div className="empty">No movement records.</div>}</div></section></Shell>}
