'use client';
import {useEffect,useMemo,useState} from 'react';import {useSearchParams} from 'next/navigation';import {Shell,CompanyNotice} from '../components';import {Product,useStore} from '../store';
const blank=(companyId:string,companyName:string):Product=>({id:'',companyId,companyName,sku:'',barcode:'',name:'',openingStock:0,received:0,shipped:0,returned:0,stock:0,minStock:25,status:'LOW STOCK',sourceFile:''});
export default function Products(){const {products,companies,selectedCompanyId,addProduct,updateProduct,removeProduct}=useStore();const searchParams=useSearchParams();const [q,setQ]=useState(searchParams.get('search')||'');useEffect(()=>{const value=searchParams.get('search');if(value!==null)setQ(value)},[searchParams]);const [company,setCompany]=useState(selectedCompanyId==='all'?'':selectedCompanyId);const [edit,setEdit]=useState<Product|null>(null);const [show,setShow]=useState(false);const filtered=useMemo(()=>products.filter(p=>(!company||p.companyId===company)&&[p.name,p.sku,p.barcode,p.companyName].join(' ').toLowerCase().includes(q.toLowerCase())),[products,q,company]);function openNew(){if(!company){alert('Select a company first.');return}const c=companies.find(x=>x.id===company)!;setEdit(blank(c.id,c.name));setShow(true)}function save(){if(!edit||!edit.sku.trim())return;if(edit.id)updateProduct({...edit,sku:products.find(p=>p.id===edit.id)?.sku||edit.sku});else addProduct({...edit,id:`${edit.companyId}::${edit.sku.trim()}`,sku:edit.sku.trim(),sourceFile:'Added in app'});setShow(false);setEdit(null)}return <Shell active="Products" title="Products"><div className="page-intro"><div><p className="eyebrow">PRODUCT MASTER</p><h1>Products</h1><p className="muted">All 86 Excel products are loaded with company-wise stock isolation.</p></div><button className="btn primary" onClick={openNew}>＋ Add Product</button></div><CompanyNotice/><div className="toolbar card"><select value={company} onChange={e=>setCompany(e.target.value)}><option value="">All Companies</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input value={q} onChange={e=>setQ(e.target.value)} placeholder="⌕ Search company, product or exact SKU..."/><span className="pill">{filtered.length} results</span></div><div className="card table-card"><div className="table-wrap"><table><thead><tr><th>Company</th><th>Product</th><th>SKU</th><th>Opening</th><th>Received</th><th>Current</th><th>Status</th><th>Action</th></tr></thead><tbody>{filtered.map(p=><tr key={p.id}><td>{p.companyName}</td><td><b>{p.name||'Unnamed product'}</b></td><td className="sku-cell">{p.sku}</td><td>{p.openingStock}</td><td>{p.received}</td><td><b>{p.stock}</b></td><td><span className={`badge ${p.stock===0?'out-of-stock':p.stock<=p.minStock?'low-stock':'in-stock'}`}>{p.stock===0?'Out of Stock':p.stock<=p.minStock?'Low Stock':'In Stock'}</span></td><td><button className="link-btn" onClick={()=>{setEdit(p);setShow(true)}}>Edit</button> <button className="link-btn danger-text" onClick={()=>removeProduct(p.id)}>Delete</button></td></tr>)}</tbody></table>{!filtered.length&&<div className="empty">No products match this filter.</div>}</div></div>{show&&edit&&<div className="modal-backdrop"><div className="modal"><div className="card-head"><div><h2>{edit.id?'Edit Product':'Add Product'}</h2><p>SKU stays exactly as imported. Existing SKUs cannot be renamed.</p></div><button className="link-btn" onClick={()=>setShow(false)}>Close</button></div><div className="form-grid"><label>Company<select value={edit.companyId} disabled={!!edit.id} onChange={e=>{const c=companies.find(x=>x.id===e.target.value)!;setEdit({...edit,companyId:c.id,companyName:c.name})}}>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Exact SKU {edit.id&&<span className="field-note">locked</span>}<input value={edit.sku} disabled={!!edit.id} onChange={e=>setEdit({...edit,sku:e.target.value})}/></label><label>Barcode (optional)<input value={edit.barcode} onChange={e=>setEdit({...edit,barcode:e.target.value})}/></label><label>Product Name<input value={edit.name} onChange={e=>setEdit({...edit,name:e.target.value})}/></label><label>Opening Stock<input type="number" min="0" value={edit.openingStock} onChange={e=>setEdit({...edit,openingStock:Number(e.target.value)})}/></label><label>Minimum Stock<input type="number" min="0" value={edit.minStock} onChange={e=>setEdit({...edit,minStock:Number(e.target.value)})}/></label></div><div className="import-note">Barcode values were not provided separately in these Excel files. Scanner matching currently supports the exact SKU and any optional barcode you add here.</div><div className="modal-actions"><button className="btn secondary" onClick={()=>setShow(false)}>Cancel</button><button className="btn primary" onClick={save}>Save Product</button></div></div></div>}</Shell>}
