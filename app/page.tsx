'use client';
import Link from 'next/link';import {Shell,Stat,CompanyNotice} from './components';import {useStore} from './store';
export default function Dashboard(){const {selectedProducts,selectedCompanyId,movements,orders,returns}=useStore();const total=selectedProducts.reduce((a,p)=>a+p.stock,0),low=selectedProducts.filter(p=>p.stock>0&&p.stock<=p.minStock).length,out=selectedProducts.filter(p=>p.stock===0).length;const healthy=selectedProducts.filter(p=>p.stock>p.minStock).length;const companyMovements=selectedCompanyId==='all'?movements:movements.filter(m=>m.companyId===selectedCompanyId);return <Shell active="Dashboard" title="Dashboard"><div className="welcome-row"><div><p className="eyebrow">OV STOCK HOUSE</p><h1>Inventory command center</h1><p className="muted">Five company inventories, marketplace activity and stock control in one place.</p></div><Link className="btn primary" href="/scan">Scan / Update →</Link></div><CompanyNotice/><div className="stats-grid"><Stat label="Total Products" value={selectedProducts.length}/><Stat label="Total Stock" value={total}/><Stat label="Low Stock Alerts" value={low} tone="orange"/><Stat label="Out of Stock" value={out} tone="red"/></div><div className="grid-2"><section className="card"><div className="card-head"><div><h2>Marketplace Activity</h2><p>Orders and returns recorded in this company view</p></div></div><div className="platform-list">{['Amazon','Flipkart','Meesho'].map(x=>{const os=orders.filter(o=>o.platform===x&&(selectedCompanyId==='all'||o.companyId===selectedCompanyId));const rs=returns.filter(r=>r.platform===x&&(selectedCompanyId==='all'||r.companyId===selectedCompanyId));return <div className="platform-row" key={x}><div className={`platform-logo ${x.toLowerCase()}`}>{x}</div><div><span>Orders</span><b>{os.length}</b></div><div><span>Items</span><b>{os.reduce((a,o)=>a+o.items,0)}</b></div><div><span>Returns</span><b className="danger-text">{rs.reduce((a,r)=>a+r.qty,0)}</b></div></div>})}</div></section><section className="card"><div className="card-head"><div><h2>Inventory Health</h2><p>Based on current stock thresholds</p></div></div><div className="health"><div className="donut"><b>{selectedProducts.length?Math.round(healthy/selectedProducts.length*100):0}%</b><span>Healthy</span></div><div className="health-legend"><span><i className="dot green-dot"/> Healthy <b>{healthy}</b></span><span><i className="dot orange-dot"/> Low Stock <b>{low}</b></span><span><i className="dot red-dot"/> Out of Stock <b>{out}</b></span></div></div></section></div><section className="card table-card"><div className="card-head"><div><h2>Recent Stock Activity</h2><p>{companyMovements.length} movement records in this view</p></div><Link href="/inventory/history">View history →</Link></div><div className="table-wrap"><table><thead><tr><th>Company</th><th>SKU</th><th>Type</th><th>Qty</th><th>Stock</th><th>Time</th></tr></thead><tbody>{companyMovements.slice(0,10).map(m=><tr key={m.id}><td>{m.companyId}</td><td><b>{m.sku}</b></td><td><span className="badge">{m.type}</span></td><td className={m.qty>=0?'success-text':'danger-text'}>{m.qty>=0?'+':''}{m.qty}</td><td>{m.stockAfter}</td><td>{new Date(m.date).toLocaleString()}</td></tr>)}</tbody></table>{!companyMovements.length&&<div className="empty">No stock movement yet. Use Scan Product or Stock In.</div>}</div></section></Shell>}
